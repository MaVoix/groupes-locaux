<div class="panel panel-default m-b-x2 text-left">
    <div class="panel-heading p-b ">
        <div class="row">
            <div class="col-xs-12 col-sm-6 col-md-6">
                <h3 id="col{{group.id}}" class="no-margin" >{{group.departement.code}} - {{ group.departement.name}}</h3>
                <div>{{ sup(group.circonscription.number,'f',false) }} circonscription</div>
            </div>
            <div class="col-xs-12 col-sm-6 col-md-6 group-amount-target-ctn">
                <a href="{{ ConfigService.get("urlSite") }}/groupe/page.html?id={{ group.id }}#budget" target="_blank">
                    <span class="btn btn-default pull-right">Objectif : <span class="badge">{{  group.amount_target_withExpenses|number_format(0, ',', ' ') }} €</span></span>
                </a>
            </div>
        </div>
        <a href="{{ ConfigService.get("urlSite") }}/groupe/page.html?id={{ group.id }}" target="_blank">Où sont-ils ? Comment les contacter ? À quoi servira l'argent récolté ?</a>
    </div>
    <div class="panel-body">


        <div class="row">
            <div class="col-xs-12 col-sm-3 col-md-3">
              <div style="background-image:url('/{{ group.path_pic_fit }}');" class="square-pic-group"></div>
          </div>
          <div class="col-md-9 col-sm-9 col-xs-12">
            <div class="row ">
              <div class="col-md-12">
                  <div class="progress mavoix ">
                      <div class="progress-bar"   role="progressbar"  style="width: {{  group.income_percent }}%">
                          <span >{{  group.income_amount|number_format(0, ',', ' ') }} €</span>
                      </div>
                      <div class="progress-bar"  role="progressbar"  style="width: {{  group.pledge_percent }}%">
                          <span >{{  group.pledge_amount|number_format(0, ',', ' ') }} €</span>
                      </div>
                  </div>
                </div>
            </div>
            <div class="row m-t-x2">

                <div class="col-sm-4 col-xs-12 text-center progress-bar-key">
                    <p><span class="label">Dons reçus</span></p>
                    <p><span class="label">Dons promis</span></p>
                </div>
                  <div class="col-sm-6 col-xs-12 text-center">
                      {%  if  (group.income_amount+group.pledge_amount)|round(2) >= group.amount_target_withExpenses|round(2) %}
                          <button type="button" class="btn btn-lg btn-primary" disabled >Objectif atteint !</button>
                      {%  else  %}
                          <a href="{{ ConfigService.get("urlSiteDons") }}/dons/participation.html?id={{ group.id }}" class="btn btn-lg btn-primary">Je cofinance cette campagne !</a>
                      {%  endif  %}
                  </div>
            </div>
          </div>



        </div>
    </div>

    <div class="row">
        <div class="col-xs-10 col-xs-offset-1">

        </div>
    </div>
</div>
