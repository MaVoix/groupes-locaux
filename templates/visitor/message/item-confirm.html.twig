<h3>{{ message.number }}<small> - {{ message.key }}</small></h3>
<div class="modal "  id="XmodalConfirm" style="display: block; padding-right: 17px;">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">×</span></button>
                <h4 class="modal-title" id="Xmodal-title">{{ message.title }}</h4>
            </div>
            <div class="modal-body" id="Xmodal-body">{{ message.text }}</div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default typo" data-dismiss="modal" id="Xmodal-cancel">{{ message.btcancel }}</button>
                <button type="button" class="btn btn-primary typo" id="Xmodal-confirm">{{ message.btconfirm }}</button>
            </div>
        </div>
    </div>
</div>